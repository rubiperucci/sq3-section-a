name: ViaCEP CI

on: [push]

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: npm install, build, and test
        run: |
          npm install
          npm test
      - name: Archive code coverage results
        uses: actions/upload-artifact@v4
        with:
          name: test-report-via-cep
          path: reports/test-report-via-cep.html
    outputs:
      artifact-id: ${{ github.run_id }}
      artifact-url: https://github.com/rubiperucci/sq3-section-a/actions/runs/${{ github.run_attempt }}/artifacts/${{ github.run_id }}